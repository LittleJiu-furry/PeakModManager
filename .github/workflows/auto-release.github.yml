name: Auto Release

on:
  workflow_dispatch:
    inputs:
      version:
        description: 'Version number (e.g., 1.0.0)'
        required: true
  push:
    tags:
      - 'v*.*.*'

jobs:
  build-frontend:
    runs-on: ubuntu-latest
    defaults:
      run:
        working-directory: ui
    steps:
      - uses: actions/checkout@v3

      - name: Setup Node.js
        uses: actions/setup-node@v3
        with:
          node-version: '22.x'
      
      - name: Setup pnpm
        uses: pnpm/action-setup@v4
        with:
          version: 8
      
      - name: Install dependencies
        run: pnpm install
      
      - name: Build frontend
        run: pnpm build
      
      - name: Upload frontend artifact
        uses: actions/upload-artifact@v7
        with:
          name: ui-build
          path: ui/dist/
          retention-days: 1
      
  build-backend:
    runs-on: windows-latest
    steps:
      - uses: actions/checkout@v3

      - name: setup uv
        uses: astral-sh/setup-uv@v7
        with:
          enable-cache: true
          version: latest
      
      - name: Install Python
        run: uv python install 3.12
      
      - name: Install dependencies
        run: uv sync --all-extras --dev
      
      - name: Install Nuitka
        run: uv pip install nuitka
      
      - name: Build backend
        run: uv run -m nuitka --standalone --windows-disable-console --assume-yes-for-downloads --output-dir=dist/ main.py
      
      - name: Upload backend artifact
        uses: actions/upload-artifact@v7
        with:
          name: backend-build
          path: dist/main.dist/
          retention-days: 1
      
  create-package:
    needs: [build-frontend, build-backend]
    runs-on: ubuntu-latest
    outputs:
      version: ${{ steps.rename-package.outputs.version }}
    steps:
      - uses: actions/checkout@v3

      - name: Download frontend artifact
        uses: actions/download-artifact@v8
        with:
          name: ui-build
          path: release/resources/ui/

      - name: Download backend artifact
        uses: actions/download-artifact@v8
        with:
          name: backend-build
          path: release/

      - name: Rename and create package
        run: |
          VERSION=${{ github.event.inputs.version }}
          if [[ -n "$VERSION" ]]; then
            version="$VERSION"
          else
            version=$(echo "${GITHUB_REF#refs/tags/}" | sed 's/^v//')
          fi
          mv release "manager-v${version}"
          zip -r release.zip "manager-v${version}/"


      - name: rename package
        id: rename-package
        run: |
          VERSION=${{ github.event.inputs.version }}
          if [[ -n "$VERSION" ]]; then
            version="$VERSION"
          else
            version=$(echo "${GITHUB_REF#refs/tags/}" | sed 's/^v//')
          fi
          mv release.zip "manager-v${version}.zip"
          echo "version=${version}" >> $GITHUB_OUTPUT
  
  publish-release:
    needs: create-package
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v3

      - name: upload artifact
        uses: actions/upload-artifact@v7
        with:
          name: manager-release
          path: manager-v${{ needs.create-package.outputs.version }}.zip
      
      - name: upload artifact
        uses: actions/upload-artifact@v7
        with:
          name: manager-release
          path: manager-v${{ needs.create-package.outputs.version }}.zip
        

